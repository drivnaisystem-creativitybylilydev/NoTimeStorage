/**
 * Human / support inbox (replies and contact). Automated mail still sends From noreply@ (see lib/email/send.ts).
 * Used in UI, legal pages, email Reply-To, and as fallback when BOOKING_NOTIFY_EMAIL is unset.
 */
export const SITE_CONTACT_EMAIL = 'notimestorage@gmail.com';
